<h2 style="margin:0 0 16px;font-size:20px;font-weight:600;color:#111;line-height:1.3;">
  {{ notification.title | e }}
</h2>

<p style="margin:0 0 20px;font-size:15px;line-height:1.6;color:#374151;">
  {{ notification.body | e }}
</p>

{% if notification.meta.event_count or notification.meta.window_start %}
<table role="presentation" cellpadding="0" cellspacing="0" border="0" style="margin:0 0 20px;background:#f9fafb;border-radius:6px;width:100%;">
  {% if notification.meta.event_count %}
  <tr>
    <td style="padding:12px 14px;font-size:13px;color:#6b7280;">
      Additional events
    </td>
    <td style="padding:12px 14px;font-size:13px;color:#111;font-family:ui-monospace,Menlo,Consolas,monospace;text-align:right;">
      {{ notification.meta.event_count | e }}
    </td>
  </tr>
  {% endif %}
  {% if notification.meta.window_start %}
  <tr>
    <td style="padding:12px 14px;font-size:13px;color:#6b7280;border-top:1px solid #e5e5e9;">
      Window start
    </td>
    <td style="padding:12px 14px;font-size:13px;color:#111;font-family:ui-monospace,Menlo,Consolas,monospace;text-align:right;border-top:1px solid #e5e5e9;">
      {{ notification.meta.window_start | e }}
    </td>
  </tr>
  {% endif %}
  {% if notification.meta.window_end %}
  <tr>
    <td style="padding:12px 14px;font-size:13px;color:#6b7280;border-top:1px solid #e5e5e9;">
      Window end
    </td>
    <td style="padding:12px 14px;font-size:13px;color:#111;font-family:ui-monospace,Menlo,Consolas,monospace;text-align:right;border-top:1px solid #e5e5e9;">
      {{ notification.meta.window_end | e }}
    </td>
  </tr>
  {% endif %}
</table>
{% endif %}

<p style="margin:0 0 24px;padding:14px;background:#dbeafe;border-left:3px solid #3b82f6;font-size:14px;color:#1e3a8a;line-height:1.5;">
  This is the <strong>summary</strong> email.  The first motion in
  this window already triggered an immediate alert; this digest
  caps the volume so you don't get a flood.  If the camera goes
  quiet, no more emails.  If motion fires again after the cooldown
  closes, you'll get a fresh "first motion" email and a new window
  starts.
</p>

<table role="presentation" cellpadding="0" cellspacing="0" border="0">
  <tr>
    <td style="background:#22c55e;border-radius:6px;">
      <a href="{{ dashboard_url }}{{ notification.link or '/dashboard' }}"
         style="display:inline-block;padding:10px 18px;font-size:14px;font-weight:600;color:#0a0a0f;text-decoration:none;">
        Review activity →
      </a>
    </td>
  </tr>
</table>
