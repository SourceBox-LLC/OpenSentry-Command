# OpenSentry Command Center Configuration

# Authentication credentials (CHANGE THESE IN PRODUCTION!)
OPENSENTRY_USERNAME=admin
OPENSENTRY_PASSWORD=opensentry

# Flask secret key (auto-generated if not set)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# SECRET_KEY=your-secret-key-here

# MQTT Broker connection
MQTT_BROKER=localhost
MQTT_PORT=1883

# MQTT authentication (must match Node credentials)
MQTT_USERNAME=opensentry
MQTT_PASSWORD=opensentry

# RTSP authentication (must match Node credentials)
RTSP_USERNAME=opensentry
RTSP_PASSWORD=opensentry
