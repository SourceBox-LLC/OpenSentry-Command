# OpenSentry Command Center Configuration
# Copy to .env and edit, or just run ./setup.sh

# Login credentials
OPENSENTRY_USERNAME=admin
OPENSENTRY_PASSWORD=changeme

# Security secret (must match camera nodes!)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
OPENSENTRY_SECRET=

# Session timeout in minutes (optional)
SESSION_TIMEOUT=30
